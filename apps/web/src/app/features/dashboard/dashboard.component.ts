import { Component } from '@angular/core';

@Component({
  selector: 'rc-dashboard',
  standalone: true,
  template: `
    <div class="dashboard-page">
      <h1>Dashboard</h1>
      <p>Welcome to ReportCanvas.</p>
    </div>
  `
})
export class DashboardComponent {}
